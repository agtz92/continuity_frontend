/**
 * @deprecated Replaced by `useNoteMutations`. The `Update` model was
 * merged into a unified `Activity` table during the slice-2 refactor.
 * This module exists only because the harness can't delete files; please
 * import `useNoteMutations` from "./useNoteMutations" instead.
 */
export {};
