import LandingPage from "@/components/marketing/LandingPage";

/** Spanish public landing (no signed-in redirect — mirrors /welcome). */
export default function WelcomePage() {
  return <LandingPage locale="es" />;
}
